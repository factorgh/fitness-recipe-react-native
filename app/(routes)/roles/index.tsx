import { StyleSheet, Text, View } from "react-native";
import React from "react";
import RoleScreen from "@/screens/roles/roles.screen";

export default function Role() {
  return <RoleScreen />;
}

const styles = StyleSheet.create({});
